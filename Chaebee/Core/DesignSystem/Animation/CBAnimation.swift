import Foundation

/// Animation duration tokens, in seconds.
/// Animation objects are intentionally not defined here.
enum CBAnimation {
    static let quickDuration: TimeInterval = 0.15
    static let standardDuration: TimeInterval = 0.25
    static let slowDuration: TimeInterval = 0.4
}
